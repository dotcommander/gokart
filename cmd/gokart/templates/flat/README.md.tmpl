# {{.Name}}

```bash
go test ./...
go run .
go build -o {{.Name}} .
```

This is an unmanaged, ordinary Go project. You own every generated file;
`gokart add` will not rewrite it. Keep process exit handling in `main`, write
command-owned output through Cobra's `OutOrStdout` and `ErrOrStderr`, and split
business logic into a package when it grows beyond the command handler.
{{- if .UseGlobal }}

`config.yaml` is created under the operating system's user configuration
directory. The generated initialization hook creates it only when absent.
{{- end }}

```bash
go build -ldflags "-X main.version=$(git describe --tags 2>/dev/null || echo dev)" -o {{.Name}} .
```
